### 2026-03-05
- 修改 src/main.py 的环境变量读取逻辑，替换为以下新变量名：
- TO_EMAIL -> EMAIL_RECIPIENT
- FROM_EMAIL -> EMAIL_SENDER
- SMTP_PASSWORD -> EMAIL_PASSWORD
- LOCATION -> CITY
- START_DATE -> LOVE_START_DATE
- SMTP_SERVER 默认值改为 smtp.qq.com
- SMTP_PORT 默认值改为 465
- 核对 GitHub Secrets: EMAIL_SENDER, EMAIL_RECIPIENT, EMAIL_PASSWORD, LOVE_START_DATE, CITY
